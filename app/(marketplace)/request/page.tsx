"use client";
import { FormEvent, useRef, useState } from "react";
const categories = [{ value: "vessel", label: "Vessels" }, { value: "energy", label: "Oil & gas" }, { value: "property", label: "Property" }, { value: "land", label: "Land" }, { value: "track_farm", label: "Agriculture" }];
const callWindows = ["Weekday mornings (09:00-12:00)", "Weekday afternoons (12:00-17:00)", "Weekday evenings (17:00-20:00)"];
export default function RequestPage() {
  const [kind, setKind] = useState<"buyer" | "seller">("buyer"); const [category, setCategory] = useState("vessel"); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submitting = useRef(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (submitting.current) return; const formElement = event.currentTarget; const form = new FormData(formElement); if (!form.getAll("call_windows").length) { setError("Select at least one preferred meeting or call window."); return; } submitting.current = true; setLoading(true); setError(""); setMessage(""); try { const response = await fetch("/api/intake", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ kind, name: form.get("name"), email: form.get("email"), phone: form.get("phone"), category, asset_type: form.get("asset_type"), imo_number: form.get("imo_number"), verification_details: form.get("verification_details"), location: form.get("location"), terms: form.get("terms"), description: form.get("description"), consent: form.get("consent") === "on", contact_consent: form.get("contact_consent") === "on", meeting_consent: form.get("meeting_consent") === "on", call_windows: form.getAll("call_windows"), timezone: form.get("timezone"), source: "public_form" }) }); const result = await response.json() as { message?: string; error?: string; whatsapp?: { sent?: boolean; error?: string } }; if (!response.ok) throw new Error(result.error ?? "Could not submit your request."); setMessage(result.message ?? "Your request has been received."); formElement.reset(); } catch (reason) { setError(reason instanceof Error ? reason.message : "Could not submit your request."); } finally { setLoading(false); submitting.current = false; } }
  const trackable = /vessel|container/i.test(category); const property = /property|land|building/i.test(category);
  return <main className="mandate-page"><div className="shell mandate-page-grid"><div><p className="eyebrow accent">Private PrimeQuest intake</p><h1>Start with a clear<br /><em>brief.</em></h1><p className="mandate-lead">Tell us what you need or what you have. PrimeQuest will review the request and coordinate the next qualified conversation.</p><div className="document-note"><strong>Category-aware review</strong><span>Vessel and container requests can be tracked by IMO number. Property and land requests should include title or COFO verification details.</span></div></div><form className="inquiry-form" onSubmit={submit}><label>Request type<select value={kind} onChange={(event) => setKind(event.target.value as "buyer" | "seller")}><option value="buyer">I am looking to buy</option><option value="seller">I have an offering</option></select></label><label>Category<select name="category" value={category} onChange={(event) => setCategory(event.target.value)} required>{categories.map((item) => <option value={item.value} key={item.value}>{item.label}</option>)}</select></label><label>Name or company<input name="name" required placeholder="Your name or company" /></label><label>Email<input name="email" type="email" required placeholder="you@company.com" /></label><label>Phone / WhatsApp<input name="phone" required placeholder="+234 ..." /></label><label>Asset or requirement<input name="asset_type" required placeholder="Vessel, container, land, building, etc." /></label>{trackable && <label>IMO number<input name="imo_number" required placeholder="IMO number for tracking" /></label>}{property && <label>Title / COFO verification details<textarea name="verification_details" required rows={3} placeholder="COFO, title, survey or verification reference" /></label>}<label>Location or preferred market<input name="location" placeholder="City, state, country or region" /></label><label>Budget or commercial terms<input name="terms" placeholder="Optional range or terms" /></label><label>{kind === "buyer" ? "What are you looking for?" : "What are you offering?"}<textarea name="description" required rows={5} placeholder="Include quantity, timing, specifications and anything important for qualification." /></label><label>Preferred timezone<select name="timezone" defaultValue="Africa/Lagos"><option value="Africa/Lagos">West Africa Time (Lagos)</option><option value="Europe/London">United Kingdom</option><option value="America/New_York">Eastern Time (US)</option><option value="Asia/Dubai">Gulf Standard Time</option></select></label><fieldset><legend>Preferred meeting or call windows</legend>{callWindows.map((window) => <label className="consent-check" key={window}><input name="call_windows" type="checkbox" value={window} /> <span>{window}</span></label>)}</fieldset><label className="consent-check"><input name="consent" type="checkbox" required /> <span>I accept the PrimeQuest terms and authorize review.</span></label><label className="consent-check"><input name="contact_consent" type="checkbox" required /> <span>I consent to contact by email, phone or WhatsApp.</span></label><label className="consent-check"><input name="meeting_consent" type="checkbox" required /> <span>I consent to an admin-mediated meeting if a legitimate match is found.</span></label>{error && <p className="form-error" role="alert">{error}</p>}{message && <p className="form-success" role="status">{message}</p>}<button className="button button-primary" type="submit" disabled={loading}>{loading ? "Sending..." : "Submit request"} <span>↗</span></button></form></div></main>;
}
